import { moduleForComponent, test } from 'ember-qunit';
//import hbs from 'htmlbars-inline-precompile';

moduleForComponent('workers-table', 'Integration | Component | workers table', {
  integration: true
});

test('pass', function(assert) {

  assert.ok(true, "implement when functionality added.");

});
